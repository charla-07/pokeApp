import { StatisticsPipe } from './statistics.pipe';

describe('StatisticsPipe', () => {
  it('create an instance', () => {
    const pipe = new StatisticsPipe();
    expect(pipe).toBeTruthy();
  });
});
